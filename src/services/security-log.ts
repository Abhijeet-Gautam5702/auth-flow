class SecurityLog {
  // Method to create a log-event of a user and project
  // Method to clear all expired logs (older than M days)
  // Method to fetch (first N) logs using userId
  // Method to get all recent failed logins of a userId
}

export const securityLog = new SecurityLog();
